package list.task2.user;

import java.util.ArrayList;

import list.task.DBConnecter;

public class UserField {

//		- 아이디 중복검사
//	boolean checkUserID = true; //falg 선언
	
	public void User checkUserID(String userId) {
		ArrayList<User> users = (ArrayList<User>) DBConnecter.Users.clone();
		
		for (int i = 0; i < users.size(); i++) {
			if(users.get(i).getId().equals(id)) {
				return users.get(i);
			}
		}
	}
		
//		- 회원가입
	
		
	
//		- 로그인 입력한 id랑 password가 user와 같을때 true값 반환
	
//		- 암호화(강사)
//		- 비밀번호 변경(마이 페이지) set사용
//		- 개인 정보 수정 set사용
//		- 인증번호 전송(API) 
	}

